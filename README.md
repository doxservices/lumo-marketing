# LUMO Studio Demo Site

Static demo website based on the supplied LUMO logo and the provided site references.

## Files
- `index.html` — full responsive demo site
- `assets/` — logo variants, favicon, and generated portfolio SVG artwork

## Usage
Open `index.html` in a browser. No build step and no external libraries required.
